openapi: "3.0.0"
info:
  title: Harmonizer
  version: 0.0.1
paths:
  /authenticate:
    get:
      operationId: routes.authenticate
      responses:
        '200':
          description: "Connexion de l'utilisateur"
          content:
            text/plain:
              schema:
                type: string
      parameters:
        - name: username
          in: query
          required: true
          schema:
            type: string
        - name: password_hash
          in: query
          required: true
          schema:
            type: string


